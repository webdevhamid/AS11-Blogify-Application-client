import { ClipLoader } from "react-spinners";

const Spinner = () => {
  return (
    <div className="max-h-full grid items-center justify-center">
      <ClipLoader color="#fb2c36" />
    </div>
  );
};

export default Spinner;
